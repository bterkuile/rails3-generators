- title t("Editing <%= singular_name %>")

= render 'form', :submit => update_button_text

= link_to show_text, <%= options[:admin] ? "[:admin, @#{singular_name}]": "@#{singular_name}" %>
\|
= link_to index_text, <%= options[:admin] ? 'admin_' : '' %><%= plural_name %>_path
