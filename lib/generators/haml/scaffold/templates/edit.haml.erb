- title t("Edit title", :scope => [:<%= plural_name %>])

= render 'form', :submit => update_button_text

= link_to show_text, <%= options[:namespace] ? "[:#{options[:namespace]}, @#{singular_name}]" : "@#{singular_name}" %>
\|
= link_to index_text, <%= options[:namespace] ? "#{options[:namespace]}_" : '' %><%= plural_name %>_path
